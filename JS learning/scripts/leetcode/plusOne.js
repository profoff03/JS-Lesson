"use strict";

var plusOne = function(digits) {
    const convertToStringDigist = digits.join("");
    const convertToIntDigits = parseInt(convertToStringDigist);
    console.log(convertToIntDigits);
};


plusOne();